# This file is replaced by db-dep-vault-fixed.yml with Vault Transit encryption
# Original deployment now uses Vault encryption-as-a-service
# Database password is encrypted with AES-256-GCM and decrypted at runtime
# Image changed from vprofile/vprofiledb to mysql:8.0.33 for better compatibility

